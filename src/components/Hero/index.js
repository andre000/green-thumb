export default {
  build() {
    this.eventHoverPlants();
  },

  eventHoverPlants() {
    // const scene = document.querySelector(".hero");
    // // eslint-disable-next-line
    // new Parallax(scene);
  }
};
